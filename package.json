{
  "name": "style-dictionary-output-reference",
  "version": "1.0.0",
  "description": "Testing including output references but omitting base or global values",
  "main": "index.js",
  "scripts": {
    "build": "node ./build.js",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nahiyan Khan",
  "license": "ISC",
  "dependencies": {
    "style-dictionary": "^3.7.1"
  }
}
